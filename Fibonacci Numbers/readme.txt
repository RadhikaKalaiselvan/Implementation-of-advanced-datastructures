Functions to compute the nth Fibonacci number and compare their running times.  Since the numbers grow fast, used BigInteger class to represent the numbers
1. Do a simple linear scan algorithm: Fib[n] = Fin[n-1] + Fin[n-2];
2. Implement O(log n) algorithm using matrix multiplication.
